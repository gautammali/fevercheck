import { HeaderStyleDirective } from './header-style.directive';

describe('HeaderStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderStyleDirective();
    expect(directive).toBeTruthy();
  });
});
